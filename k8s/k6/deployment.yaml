---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: k6-loadtest
spec:
  replicas: 1
  selector:
    matchLabels:
      app: k6
  template:
    metadata:
      labels:
        app: k6
    spec:
      containers:
        - name: k6
          image: grafana/k6:latest
          args: ['run', '/scripts/test.js']
          volumeMounts:
            - name: k6-scripts
              mountPath: /scripts
      restartPolicy: Always
      volumes:
        - name: k6-scripts
          configMap:
            name: k6-script-config
